services:
  coordinator:
    build:
      context: ./services/coordinator
    container_name: fyp-coordinator
    ports:
      - "8001:8000"
    env_file:
      - ./services/coordinator/.env
    volumes:
      - ./web:/app/web:ro
    restart: unless-stopped
